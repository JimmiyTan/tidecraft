# lobster-farm 使用说明书

## 1. 项目用途

`lobster-farm` 是一个本地运行的短视频内容生产与人工审核演示系统。它可以展示：

1. 输入一个内容主题
2. 生成 5 条候选选题与脚本
3. 生成审核消息和视频 mock 结果
4. 模拟人工审核通过
5. 生成抖音、视频号发布准备包
6. 模拟写入人工发布回执
7. 归档任务并保留审计记录

项目不自动登录平台，也不自动正式发布内容。

## 2. 推荐运行环境

- Windows 10 或 Windows 11
- Python 3.10 或更高版本
- 项目位于本地目录中
- 浏览器可访问 `http://127.0.0.1:8765`

首次使用前应按照 `docs/install.md` 准备 Python 环境。

## 3. 快捷按钮

项目根目录提供以下可双击按钮：

| 按钮 | 用途 | 是否联网 |
| --- | --- | --- |
| `一键启动客户演示.cmd` | 启动浏览器客户演示页面 | 否 |
| `一键停止客户演示.cmd` | 停止本地客户演示服务 | 否 |
| `一键运行演示.cmd` | 在命令行执行完整安全 Demo | 否 |
| `一键完整验证.cmd` | 执行全部 smoke 和 unittest | 否 |
| `一键隐私检查.cmd` | 检查上传候选文件与 Git 历史 | 否 |
| `一键GitHub上传前检查.cmd` | 执行隐私、测试、差异和忽略规则总检查 | 否 |
| `打开使用说明书.cmd` | 打开本说明书 | 否 |

这些按钮不会执行 `git add`、`git commit` 或 `git push`。

## 4. 客户演示操作

### 4.1 启动

双击 `一键启动客户演示.cmd`。

正常情况下浏览器会打开：

```text
http://127.0.0.1:8765
```

如果浏览器没有自动打开，可手动复制该地址访问。

### 4.2 演示流程

1. 在“客户演示主题”中输入客户行业或场景
2. 在“演示操作人”中填写演示人称呼
3. 点击“开始客户演示”
4. 等待页面完成五步流程
5. 展示候选选题、双平台交付状态和安全边界

推荐客户主题示例：

```text
连锁咖啡品牌如何用 AI 提升短视频内容效率
```

### 4.3 停止

优先在启动窗口按 `Ctrl+C`。

也可以双击 `一键停止客户演示.cmd`。停止脚本只会停止命令行中包含 `customer_demo_server.py` 的本项目演示进程；如果端口被其他程序占用，脚本会拒绝停止该程序。

## 5. 安全默认值

客户演示固定使用：

```text
RUN_MODE=dry-run
FEISHU_ADAPTER=dry-run
VIDEO_PROVIDER=mock
```

演示入口读取 `.env.example`，不读取真实 `.env`。如果模板中出现非空凭据，Demo 会拒绝运行。

客户演示服务只监听 `127.0.0.1`，不会向局域网或公网开放。页面不加载外部脚本、字体、图片或统计服务。

## 6. 运行数据位置

运行后会在以下目录生成本地数据：

- `data/`：状态、队列和临时数据
- `exports/`：审核包、发布准备包和演示摘要
- `logs/`：运行日志

以上目录已加入 `.gitignore`，不应上传 GitHub。

不要整体删除运行目录。需要清理时，应先备份，再按 `task_id` 精确处理。

## 7. GitHub 上传前操作

双击 `一键GitHub上传前检查.cmd`，它会依次执行：

1. 隐私与密钥扫描
2. 完整项目验证
3. `git diff --check`
4. `.env`、`data/`、`exports/`、`logs/` 忽略状态确认
5. 显示最终 Git 工作树状态

总检查通过后，再人工阅读：

- `PRIVACY_AUDIT.md`
- `GITHUB_UPLOAD_CHECKLIST.md`
- `PROJECT_CLOSURE.md`

上传前必须由项目负责人复核即将暂存的准确文件列表。禁止直接使用 `git add .`。

检查脚本兼容 Windows PowerShell 5 和 PowerShell 7，并且不会使用 `ExecutionPolicy Bypass`。

当前项目已配置 GitHub 公开远程仓库。后续再次提交、推送或调整仓库可见性，仍需单独获得明确批准。

项目当前没有开源许可证，因此代码公开可见不等于已获得明确的开源使用许可。若后续需要允许复制、修改或分发，许可证类型必须由项目负责人另行决定。

## 8. 隐私保护规则

不得上传：

- `.env` 或任何真实环境配置
- App Secret、API Key、Access Token、Refresh Token
- Webhook、Cookie、账号密码或私钥
- 客户名称、手机号、邮箱或内部资料
- `data/`、`exports/`、`logs/` 中的真实运行数据
- 浏览器个人数据或平台登录信息

自动检查只能识别已知模式，不能替代人工审核。如果凭据曾经进入 Git 历史，即使当前文件已删除，也必须先轮换凭据并清理历史。

## 9. 常见问题

### 9.1 端口 8765 被占用

先双击 `一键停止客户演示.cmd`。如果提示是其他程序占用，请不要强制结束该程序，可改用：

```powershell
.\scripts\start-customer-demo.ps1 -Port 8877
```

### 9.2 找不到 Python

按照 `docs/install.md` 准备 Python 3.10+，或在项目根目录创建 `.venv`。

### 9.3 页面显示演示失败

先双击 `一键完整验证.cmd`，根据失败步骤检查本地环境。演示不会把详细内部错误返回给浏览器。

### 9.4 PowerShell 脚本被执行策略阻止

不要绕过公司安全策略。请让管理员允许当前项目的受控脚本，或者在已批准的 PowerShell 环境中手动执行对应命令。

## 10. 回滚

源码回滚应基于 Git 差异逐文件执行，不使用强制重置覆盖未提交工作。

运行数据回滚应先备份 `.env`、`data/`、`exports/` 和 `logs/`，再参考 `docs/backup-and-restore.md`。
