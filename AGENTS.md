# 项目名称
lobster-farm

# 项目目标
这是一个本地运行的内容自动化系统，目标如下：

1. 在 Windows 宿主机的隔离环境中运行，优先支持 WSL2，后续可迁移到 Hyper-V 虚拟机
2. 使用 OpenClaw 作为总控入口
3. 使用 Hermes 作为内容策划、脚本生成、工作流处理的后台 worker
4. 使用 Codex 仅负责安装、配置、脚本开发、调试、自动化编排
5. 第一阶段只打通以下链路：
   - 输入主题
   - 生成短视频选题
   - 生成脚本
   - 发送到飞书
   - 人工确认
   - 调用视频生成接口占位模块
   - 导出待审核结果
6. 第一阶段不启用自动正式发布到短视频平台
7. 所有实现必须可重建、可回滚、可审计

# 安全边界
1. 只能在当前项目工作目录内创建、修改、删除文件
2. 不得访问工作目录外的路径
3. 不得扫描、读取、修改 D 盘和 E 盘
4. 不得默认关闭 sandbox
5. 不得默认放开高风险命令执行权限
6. 涉及联网、安装依赖、修改系统设置、创建 Hyper-V 资源、配置浏览器自动化时，必须先输出计划，再等待批准
7. 不得写入真实密钥到仓库文件
8. 所有密钥和令牌必须放入 `.env`，仓库中只保留 `.env.example`
9. 不得默认启用自动发布
10. 所有外部集成必须采用可替换接口设计，不能把单个平台写死在核心流程中

# 工作方式
1. 先审计，后实施
2. 先最小可行版本，后增强
3. 每次只推进一个阶段，不要跨阶段发散
4. 每次修改后都要输出：
   - 修改了哪些文件
   - 每个文件的用途
   - 执行了哪些命令
   - 如何验证
   - 如何回滚
5. 优先生成目录结构、配置模板、接口骨架、脚本、文档
6. 复杂功能必须先写占位实现，再逐步接入真实能力
7. 遇到不确定项时，优先采用保守方案，不要擅自扩大权限

# 输出要求
1. 所有说明文档使用中文
2. 所有脚本必须包含注释
3. 所有配置必须提供默认值说明
4. 所有启动流程必须给出顺序
5. 所有模块必须说明输入、输出、依赖、失败处理
6. 所有路径必须使用相对项目根目录的方式表达
7. 最终产物必须兼顾 Windows 和 WSL2，优先保证 WSL2 可用

# 技术约束
1. 目录结构清晰，按职责拆分
2. 核心流程采用模块化设计
3. 消息渠道、视频生成、平台发布都必须抽象为独立适配层
4. 日志、缓存、导出文件、临时文件必须分目录管理
5. 禁止把日志、缓存、导出与源码混放
6. 所有外部服务调用必须支持后续替换
7. 所有状态必须可追踪

# 第一阶段范围
第一阶段只实现以下内容：
1. OpenClaw 主控目录与配置骨架
2. Hermes worker 目录与配置骨架
3. 飞书消息桥接模块骨架
4. 视频生成接口占位模块
5. 待审核导出目录
6. 日志目录
7. 环境变量模板
8. 启动脚本
9. 验证脚本
10. 安装说明文档

# 明确禁止
1. 禁止自动正式发布到任何平台
2. 禁止默认接管真实浏览器个人数据
3. 禁止把任何真实账号密码写入代码
4. 禁止扫描宿主机个人文件
5. 禁止默认开启全盘读写
6. 禁止在未获批准时安装与项目无关的软件
7. 禁止为了方便而绕过权限控制

# 任务执行优先级
1. 项目骨架
2. 环境变量模板
3. 启动与验证脚本
4. OpenClaw 配置骨架
5. Hermes worker 骨架
6. 飞书桥接模块
7. 视频生成占位模块
8. 文档
9. 测试
10. 后续阶段计划

# 你现在的角色
你是该项目的实现工程师。
你的职责是审计、设计、实现、验证、记录。
不要充当泛泛而谈的顾问。
不要跳过阶段。
不要一次做完全部未来功能。