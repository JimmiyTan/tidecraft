# 隐私与密钥审计报告

## 审计范围

审计对象为：

- Git 已跟踪文件
- 当前未被 `.gitignore` 排除的未跟踪文件
- 现有 Git 历史补丁中的已知密钥格式

不会读取或输出真实 `.env` 内容，也不会扫描项目目录外的文件。

## 自动检查项

- 私钥头、GitHub Token、AWS Access Key、OpenAI 风格密钥和 Slack Token
- URL 内嵌用户名与密码
- 配置文件中的非空敏感字段
- Python 敏感变量中的直接硬编码字符串
- `.env` 变体、私钥、证书密钥和常见凭据文件名
- 邮箱、常见中国大陆手机号和 Windows 用户目录
- Git 历史中的已知高风险密钥格式

## 2026-08-16 检查结果

- 高风险发现：0
- 需人工确认发现：0
- 上传候选文件：206
- Git 远程仓库：1 个，目标为 GitHub 公开仓库
- 候选文件中的本项目绝对路径与 Windows 用户目录：未发现
- 结论：当前自动规则未发现隐私或密钥泄露迹象

运行命令：

```powershell
python .\scripts\privacy-audit.py
```

或双击：

```text
一键隐私检查.cmd
```

## 已确认的保护措施

- `.env`、`.env.*` 默认忽略，仅允许 `.env.example`
- `data/`、`exports/` 和 `logs/` 默认忽略
- 私钥、证书密钥、凭据目录、包管理器凭据、浏览器会话、本地数据库和 PID 文件默认忽略
- 客户前端接口不返回本地路径、演示链接或凭据字段
- 客户演示固定读取 `.env.example`
- 客户演示只监听 `127.0.0.1`
- 页面不加载外部统计、字体、脚本或图片

## 自动审计边界

自动规则无法保证识别所有自定义格式、编码后的秘密、业务敏感文案、图片中的个人信息或不符合常见格式的内部标识。

因此，上传前仍必须：

1. 按 `GITHUB_UPLOAD_CHECKLIST.md` 人工复核
2. 精确检查即将暂存的每个文件
3. 暂存后检查完整 cached diff
4. 确认 Git 历史中没有曾经有效的凭据

如果任何真实凭据曾进入 Git 历史，必须立即轮换凭据。仅从当前文件删除不能消除历史泄露风险。
